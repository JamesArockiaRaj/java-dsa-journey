# Day 02 Notes

## Arrays
- Arrays start from index 0.
- User choice can be converted to array index using `choice - 1`.

## Time Complexity
- `O(n)`: Checks each element one by one.
- `O(1)`: Directly accesses an element using its index.
